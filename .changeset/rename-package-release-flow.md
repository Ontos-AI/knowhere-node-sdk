---
'@ontos-ai/knowhere-sdk': minor
---

Rename the published SDK package to `@ontos-ai/knowhere-sdk` and move releases to a Changesets-managed release PR workflow with manual beta snapshot publishes.
